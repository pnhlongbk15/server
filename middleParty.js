
module.exports = app => {
        // cors
        // const cors = require('cosrs')
        // app.use(cors({
        //         origin: '*'
        // }))

        // content-type access
        app.use(express.json());

}